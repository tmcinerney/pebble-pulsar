name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-validate:
    name: Build & Validate Suite PBWs
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Nix
        uses: cachix/install-nix-action@v31
        with:
          extra_nix_config: |
            experimental-features = nix-command flakes

      - name: Install Cachix
        uses: cachix/cachix-action@v16
        with:
          name: devenv

      - name: Install Devenv
        run: nix profile install --accept-flake-config github:cachix/devenv/latest

      - name: Run Firmware & Configuration Tests
        run: |
          devenv shell -- python3 tests/test_firmware.py

      - name: Build Monorepo Suite Bundles
        run: |
          devenv shell -- python3 scripts/build_all.py
          python3 scripts/validate_pbw.py

      - name: Upload PBW Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: pebble-pulsar-suite-pbws
          path: dist/*.pbw
          retention-days: 14
